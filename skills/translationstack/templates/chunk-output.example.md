# 示例分块译文

这是 `translations/chunks/<chunk-id>.md` 的最小示例。翻译子代理写 Markdown；合并/整合阶段再生成同名 JSONL wrapper。

## 第一段

这里是译文正文。保留 Markdown 结构、脚注标记[^1]、图片链接、代码块、引用块和行尾硬换行。

> 若源文是引用块，译文也保留引用块格式。

[^1]: 脚注标记和编号必须与源文保持一一对应。
